# Project initialization
    # git init
    # git add .
    # git commit -m "First Commit"
    # git branch -M main
    # git remote (Your repository link)
    # git push -u origin main

# Update project
    # git add .
    # git commit -m "Second commit"
    # git push origin main

# React command line
    # npm create vite@latest
    # npm install
    # npm run dev